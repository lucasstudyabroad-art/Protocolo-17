// Intentionally empty: each subproject applies its own plugins (see module build.gradle.kts
// files). Declaring the Android Gradle Plugin here would force its resolution even for
// pure-JVM modules (:protocol-core, :pos-simulator) that never need it.
